{% set field_errors = errors[field_name]|default([]) %}
{% if field_errors is not empty %}
    <ul class="mt-1 text-xs text-red-600 dark:text-red-400 space-y-1">
        {% for error in field_errors %}
            <li>{{ error }}</li>
        {% endfor %}
    </ul>
{% endif %}
